# ═══════════════════════════════════════════════════════════════
#                    TrendRadar 配置文件 (关键词监控版)
# ═══════════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────────
#                          通用配置
# ───────────────────────────────────────────────────────────────
common:
  enable_frequency_words: true          # 启用关键词过滤
  frequency_words_file: config/frequency_words.txt
  enable_keyword_highlight: true        # 高亮关键词
  keyword_highlight_color: "#FF6B6B"   # 关键词高亮颜色
  min_keyword_score: 1                  # 最小关键词匹配数(至少匹配1个关键词)
  
# ───────────────────────────────────────────────────────────────
#                          数据源配置 (全部禁用榜单)
# ───────────────────────────────────────────────────────────────
sources:
  # 微博热搜 - 仅关键词监控
  weibo_hot:
    enabled: true
    name: 微博热搜
    require_keyword_match: true         # 必须匹配关键词才推送
    min_hot_index: 100000               # 提高热度阈值,减少噪音
    max_items: 20                       # 限制每次最多推送20条
    
  # 知乎热榜 - 仅关键词监控
  zhihu_hot:
    enabled: true
    name: 知乎热榜
    require_keyword_match: true
    min_hot_index: 500000
    max_items: 20
    
  # 百度热搜 - 仅关键词监控
  baidu_hot:
    enabled: true
    name: 百度热搜
    require_keyword_match: true
    min_hot_index: 100000
    max_items: 20
    
  # 抖音热榜 - 仅关键词监控
  douyin_hot:
    enabled: true
    name: 抖音热榜
    require_keyword_match: true
    min_hot_index: 500000
    max_items: 20
    
  # 36氪 - 科技资讯
  36kr:
    enabled: true
    name: 36氪
    require_keyword_match: true
    max_items: 15
    
  # IT之家 - 科技资讯
  ithome:
    enabled: true
    name: IT之家
    require_keyword_match: true
    max_items: 15
    
  # 少数派 - 效率工具
  sspai:
    enabled: true
    name: 少数派
    require_keyword_match: true
    max_items: 10
    
  # V2EX - 技术社区
  v2ex:
    enabled: true
    name: V2EX
    require_keyword_match: true
    max_items: 15
    
  # 虎嗅 - 商业科技
  huxiu:
    enabled: true
    name: 虎嗅
    require_keyword_match: true
    max_items: 10
    
  # ───────────────────────────────────────────────────────────
  # 以下源禁用或根据需求开启
  # ───────────────────────────────────────────────────────────
  
  # 微信读书 - 如不需要可禁用
  weread:
    enabled: false
    
  # 什么值得买 - 购物相关,建议禁用
  smzdm:
    enabled: false
    
  # 贴吧热议 - 噪音较多,建议禁用
  tieba_hot:
    enabled: false
    
  # 哔哩哔哩 - 娱乐内容较多
  bilibili_hot:
    enabled: false
    
  # 今日头条 - 新闻源,可选开启
  toutiao:
    enabled: false

# ───────────────────────────────────────────────────────────────
#                          推送配置
# ───────────────────────────────────────────────────────────────
notification:
  # 飞书推送配置
  feishu:
    enabled: true
    webhook_url: "你的飞书webhook地址"  # ⚠️ 必须填写
    
    # 推送格式配置
    message_template: |
      **🔥 {source} 关键词监控**
      
      **标题**: {title}
      **热度**: {hot_index}
      **匹配关键词**: {matched_keywords}
      **时间**: {timestamp}
      **链接**: {url}
      
      ---
    
    # 每日摘要配置
    daily_summary:
      enabled: true
      time: "08:00"                    # 每天8点推送摘要
      include_keyword_stats: true       # 包含关键词统计
      max_items_per_keyword: 5          # 每个关键词最多显示5条

    # 实时推送配置 (可选)
    realtime:
      enabled: false                    # 关闭实时推送,只用每日摘要
      min_interval_minutes: 60          # 如开启,至少间隔60分钟推送一次

# ───────────────────────────────────────────────────────────────
#                          调度配置
# ───────────────────────────────────────────────────────────────
schedule:
  # 数据采集频率
  crawl_interval_hours: 2              # 每2小时抓取一次数据
  
  # 推送时间窗口
  push_time_windows:
    - start: "08:00"
      end: "22:00"                     # 只在8:00-22:00推送
  
  # 节假日配置
  skip_holidays: false                 # 节假日也推送
  timezone: "Asia/Shanghai"

# ───────────────────────────────────────────────────────────────
#                          过滤与降噪
# ───────────────────────────────────────────────────────────────
filter:
  # 去重配置
  deduplication:
    enabled: true
    time_window_hours: 24              # 24小时内相同标题去重
    similarity_threshold: 0.8          # 相似度>80%视为重复
  
  # 质量过滤
  quality:
    min_title_length: 10               # 标题至少10字
    max_title_length: 100              # 标题最多100字
    block_keywords:                    # 黑名单关键词
      - 明星
      - 娱乐
      - 八卦
      - 恋情
      - 离婚
      - 出轨
      - 网红
      - 直播
      - 带货
      - 星座
      - 占卜
      - 风水
      - 震惊
      - 标题党
      - 广告
      - 推广
      - 代写
      - 兼职
      - 刷单

# ───────────────────────────────────────────────────────────────
#                          存储配置
# ───────────────────────────────────────────────────────────────
storage:
  database:
    type: sqlite                       # 使用 SQLite 本地存储
    path: data/trendradar.db
  
  # 数据保留策略
  retention:
    keep_days: 30                      # 保留30天数据
    auto_cleanup: true

# ───────────────────────────────────────────────────────────────
#                          日志配置
# ───────────────────────────────────────────────────────────────
logging:
  level: INFO
  file: logs/trendradar.log
  max_size_mb: 10
  backup_count: 5
